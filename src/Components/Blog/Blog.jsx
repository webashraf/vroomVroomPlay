import React from 'react';
import useTitle from '../../hooks/useTitle';

const Blog = () => {
    useTitle("Blogs")
    return (
        <div>
            
        </div>
    );
};

export default Blog;